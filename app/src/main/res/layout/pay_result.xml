<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:id="@+id/pay_result"
    android:gravity="center_horizontal">

    <View
        android:id="@+id/pay_result_statusBar"
        android:layout_width="match_parent"
        android:layout_height="0dp" />

    <ImageView
        android:id="@+id/pay_result_img"
        android:layout_width="@dimen/dp_80"
        android:layout_height="@dimen/dp_80"
        android:layout_marginTop="169dp"
        android:scaleType="centerCrop"
        android:src="@mipmap/pay_success" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textStyle="bold"
        android:layout_marginTop="@dimen/dp_26"
        android:textColor="#333333"
        android:textSize="@dimen/sp_18"
        android:text="支付成功" />

    <TextView
        android:id="@+id/pay_result_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginTop="@dimen/dp_32"
        android:textColor="#333333"
        android:textSize="@dimen/sp_14"
        android:text="3秒后自动跳转," />

    <com.yijiuyiyiedu.xuetang.widget.StateButton
        android:id="@+id/pay_result_see"
        android:layout_width="@dimen/dp_200"
        android:layout_marginTop="90dp"
        android:layout_height="@dimen/dp_40"
        app:radius="5dp"
        app:normalStrokeWidth="1dp"
        app:normalStrokeColor="#773685"
        app:normalBackgroundColor="#ffffff"
        android:text="立即观看" />


</LinearLayout>